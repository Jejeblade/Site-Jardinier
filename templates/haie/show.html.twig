{% extends 'base.html.twig' %}

{% block title %}Haie{% endblock %}

{% block body %}
{% block content %}
    <h1>Haie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ haie.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ haie.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ haie.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_haie_index') }}">back to list</a>

    <a href="{{ path('app_haie_edit', {'code': haie.code}) }}">edit</a>

    {{ include('haie/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}